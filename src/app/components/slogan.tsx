'use client';

import { Typography } from 'antd';

const { Title } = Typography;

const Slogan = () => {
  return (
    <Title level={2} className='text-center mb-4'>RUMIS</Title>
  );
}

export { Slogan };
